import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.templates',
  appName: 'capy_food_app',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
